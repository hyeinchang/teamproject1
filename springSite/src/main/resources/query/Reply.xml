<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.client.reply.dao.ReplyDao">
<select id="replyList" parameterType="reply" resultType="reply">
	select r_num, b_num, r_name, r_content, r_pwd,
	to_char(r_date,'yyyy-mm-dd hh24:mi:ss') as r_date from spring_reply
	where b_num = #{b_num} order by r_num desc
</select>
<insert id="replyInsert" parameterType="reply">
	<selectKey keyProperty="r_num" resultType="int" order="BEFORE">
		select spring_reply_seq.nextval from dual
	</selectKey>
	insert into spring_reply (r_num, b_num, r_name, r_content, r_pwd) 
	values(#{r_num}, #{b_num}, #{r_name}, #{r_content}, #{r_pwd})
</insert>
<select id="replyPwdConfirm" parameterType="reply" resultType="int">
	select nvl((select 1 from spring_reply where r_num = #{r_num} and r_pwd = #{r_pwd}),0) as status
	from dual
</select>
<update id="replyUpdate" parameterType="reply">
	update spring_reply set r_content = #{r_content},
	r_date = sysdate where r_num = #{r_num}
</update>
<delete id="replyDelete" parameterType="reply">
	delete from spring_reply where r_num = #{r_num}
</delete>
<delete id="replyChoiceDelete" parameterType="reply">
	delete from spring_reply where b_num = #{b_num}
</delete>
</mapper>